# Architecture

Browser:
`input -> controller -> reduced flight dynamics -> thermal/FSI visualization -> telemetry`

Reference Python:
`geometry/mass constants -> rigid-body gyro regression`

The browser has no external libraries or network dependencies.
